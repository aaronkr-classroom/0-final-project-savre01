<!-- views/users/index.ejs -->

<!--
  Listing 18.8 (p. 268)
  index.js에서의 모든 사용자 리스팅
-->

<main class="">
    <div class="container col-xl-10 col-xxl-8 px-4 py-5">
      <div class="pt-5 pb-5">
        <h1>Games</h1>
        <p>Here are all the games on our site.</p>
  
        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Pic</th>
              <th>Gamename</th>
              <th>User Name</th>
              <th>Email</th>
              <th>Subscribed?</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% games.forEach(function(game, i) { %>
            <tr>
              <td><%= i+1 %></td>
              <td>
                <a href="/games/<%= game.id %>">
                  <figure class="game-img">
                    <img src="<%= game.profileImg ||
                    "https://via.placeholder.com/150" %>" alt="Placeholder image"
                    class="rounded-circle" />
                  </figure> </a
                ><!-- HTML에서 사용자 이금과 ID 삽입 -->
              </td>
              <td>
                <a href="/games/<%= game.id %>"><%= game.gamename %></a
                ><!-- HTML에서 사용자 이금과 ID 삽입 -->
              </td>
              <!-- 
                Listing 19.6 (p. 284)
                name 데이터 업데이트
              -->
              <td><%= game.fullName %></td>
              <td><%= game.email %></td>
              <% if (game.subscribedAccount) { %>
              <!-- 구독 계정이 있는 경우 -->
              <td><%= game.subscribedAccount %> ✅</td>
              <% } else { %>
              <!-- 구독 계정이 없는 경우 -->
              <td>❌</td>
              <% } %>
              <td>
                <!-- 
                  Listing 20.2 (p. 291)
                  사용자 편집을 위한 테이블 수정
                -->
                <a
                  class="btn btn-sm btn-outline-primary"
                  role="button"
                  href="/games/<%= game._id %>/edit"
                  >✏️ Edit</a
                ><!-- 사용자 ID를 edit 태그 링크에 삽입 -->
  
                <!-- 
                  Listing 20.8 (p. 297) 
                  사용자 삭제 링크
                -->
                <a
                  class="btn btn-sm btn-outline-danger"
                  role="button"
                  href="/games/<%= game._id %>/delete?_method=DELETE"
                  onclick="return confirm('Are you sure you want to delete this record?')"
                  >🗑️ Delete</a
                ><!-- 인덱스 페이지에서 delete 액션으로의 링크 추가 -->
  
                <!--
                  노트: onclick="return confirm('Are you sure you want to delete this record?')"의
                  추가로 좀 더 안전한 장치를 추가할 수 있다.
                -->
              </td>
            </tr>
            <% }); %>
          </tbody>
        </table>
  
        <div class="mt-5 text-center">
          <a href="/games/new" class="btn btn-primary">Add New Game</a>
          <a
            href="/"
            class="btn ut-red disabled"
            role="button"
            aria-disabled="true"
            >Delete All Games</a
          >
          <a
            href="/"
            class="btn btn-secondary disabled"
            role="button"
            aria-disabled="true"
            >Link Subscribers</a
          >
        </div>
      </div>
    </div>
  </main>